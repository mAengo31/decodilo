# Lambda File Transfer Prohibition

M053 prohibits file transfer for future SSH-connectivity-only review. `scp`,
`sftp`, `rsync`, uploads, and downloads are all out of scope.

Future command previews must not include file-transfer flags or commands.
